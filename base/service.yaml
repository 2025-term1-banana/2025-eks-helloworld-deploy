apiVersion: v1
kind: Service
metadata:
  name: flask-hello
  namespace: flask-hello
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
  selector:
    app: flask-hello
